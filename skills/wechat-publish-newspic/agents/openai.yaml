interface:
  display_name: "WeChat Newspic Publisher"
  short_description: "Complete and publish WeChat image drafts"
  default_prompt: "Use $wechat-publish-newspic to collect missing image-post details, validate the payload, and publish a WeChat newspic draft."
